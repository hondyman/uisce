# Dependencies
/node_modules
/vendor

# Build artifacts
/build
/dist/
/dist
/.out/

# Go build outputs (compiled binaries from go build)
api-gateway/api-gateway
api-gateway/api-gateway-bin
api-gateway/server
backend/app./cmd/server
backend/cmd/*/main
backend/e2e_amqp
backend/rule-engine-service
backend/semantic-rules-api
backend/semantic-rules-api/semantic-rules-api
backend/temporal-worker
backend/validation-service
backend/websocket
backend/websocket_server
bp-backend/server
calendar-service/bin/calendar-service
migrate
migration
server
services/ai-builder/ai-builder
services/compliance-engine/compliance-engine
services/governance/governance
services/governance/governance-service
services/semantic-engine/cmd/cmd
services/semantic-engine/semantic-engine

# Backend runtime artifacts
backend.log
*.pid
config.yaml

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.*.local
.env.mesh

# IDE and editor files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln

# OS-generated files
.DS_Store
Thumbs.db

# Go test binaries
*.test

# Local logs and tmp dirs
/logs/
/tmp/

# Compiled service binaries (untracked)
/apps/analytics-api/analytics-api
/apps/genui-api/genui-api
/apps/orchestration-api/orchestration-api
/backend/apps/
/backend/cmd/audit-worker/audit-worker
/backend/cmd/event-router/event-router
/backend/cmd/outbox-processor/outbox-processor
/backend/cmd/snapshot-worker/snapshot-worker
/platform_server
/rules_server
/semlayer
/trading-consumer
/notifications-service
/uisce-mcp-server
/semantic-sandbox
/semctl
/provision_tenant
/load_wealth_management_bundle
/load_wealth_management_super_bundle
/load_generic_bundle
/demo
/api.test
/fabric-builder

# Stale diagnostic outputs
/frontend/eslint-report.json
/frontend/eslint-warnings.json

# Vite and tool artifacts
*.bak
*.backup
*.bak.*
*.OLD
*.old
*.BACKUP
*.swp
*.swo
*.tmp
*.orig
*.rej
*.before_fix.bak
.env.before_fix.bak
schema_backup_*.sql
vite.config.ts.timestamp-*.mjs

# Block accidental nested clones of the frontend repo (consolidated 2026-07-30)
uisce_frontend/

# Block zero-byte shell-glob artifacts created by unquoted cp/mv (e.g. `[ai-builder]`)
/[*
datafusion-engine/target/

# Go devjwt binary
backend/devjwt

# `go build ./cmd/<name>` (or `go run .` in some setups) without -o drops
# a binary named after the directory into wherever the invoking cwd was -
# backend/, backend/cmd/<name>/, or backend/rule-engine/, depending on how
# it was run. This has produced many accidentally-committed stray
# binaries across the repo (backend/bp-starter, backend/catalog-admin,
# backend/cmd/bundles/bundles, ...; backend/devjwt above is one instance
# of the same problem, patched one-off). General rule instead of another
# one-off: an extensionless file directly in backend/cmd/*/ or
# backend/rule-engine/ is a binary, never source - except Dockerfile.
# (Ignore all direct children, then un-ignore anything with a dot in its
# name and any subdirectory - a plain "no-dot filename" glob isn't
# expressible directly in gitignore's pattern language.)
backend/cmd/*/*
!backend/cmd/*/*.*
!backend/cmd/*/*/
!backend/cmd/*/Dockerfile
backend/rule-engine/*
!backend/rule-engine/*.*
!backend/rule-engine/*/
!backend/rule-engine/Dockerfile
# backend/rule-engine/cmd/<name>/ is its own nested cmd/ tree (separate
# from top-level backend/cmd/*/ above) - missed on the first pass of this
# rule, which is exactly how generate-monaco/generate-types binaries got
# committed here mid-session even after the first fix.
backend/rule-engine/cmd/*/*
!backend/rule-engine/cmd/*/*.*
!backend/rule-engine/cmd/*/*/
!backend/rule-engine/cmd/*/Dockerfile
