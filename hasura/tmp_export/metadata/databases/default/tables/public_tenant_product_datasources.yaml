table:
  name: tenant_product_datasources
  schema: public
object_relationships:
- name: tenant_product
  using:
    foreign_key_constraint_on: tenant_product_id
- name: alpha_tenant_instance
  using:
    foreign_key_constraint_on: datasource_id
