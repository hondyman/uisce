package models

// placeholder: HTTP handlers for guardrails live under internal/handlers.
// This file remains as a benign placeholder in package `models` so that the
// `backend/models` package compiles cleanly during refactors that move
// implementations to internal packages.
