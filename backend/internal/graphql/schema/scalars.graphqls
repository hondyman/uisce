# Common scalar types and base types used across all schemas
scalar UUID
scalar Timestamp
scalar JSON

# Base Query and Mutation types - extended by other schemas
type Query {
  _empty: String
}

type Mutation {
  _empty: String
}
