package cube

// Package-level config instance (equivalent to Python's config object)
var GlobalConfig = GetConfig()

// Package-level template context instance
var GlobalTemplate = GetGlobalTemplateContext()
