import { IComparable } from "../utils/helpers.js";
import { EqualsFunction, HashFunction } from "./HashSet.js";
interface Entry<Key extends IComparable, Value> {
    key: Key;
    value: Value;
}
export declare class HashMap<Key extends IComparable, Value> {
    private data;
    private hashFunction;
    private equalsFunction;
    constructor(hashFunction?: HashFunction, equalsFunction?: EqualsFunction);
    set(key: Key, value: Value): Value;
    containsKey(key: Key): boolean;
    get(key: Key): Value | null;
    entries(): Array<Entry<Key, Value>>;
    getKeys(): Key[];
    getValues(): Value[];
    toString(): string;
    get length(): number;
}
export {};
