// React import removed (not needed with the new JSX transform)
import CoreCustomEditorPrototype from '../components/Prototypes/CoreCustomEditorPrototype';
import '../components/Prototypes/CoreCustomEditor.css';

export default {
  title: 'Prototypes/CoreCustomEditor',
  component: CoreCustomEditorPrototype,
};

export const Default = () => <CoreCustomEditorPrototype />;
