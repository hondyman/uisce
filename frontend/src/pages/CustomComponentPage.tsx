import type React from 'react';
import { CustomComponentManager } from '../components/CustomComponentManager/CustomComponentManager';

export const CustomComponentPage: React.FC = () => {
  return <CustomComponentManager />;
};

export default CustomComponentPage;
