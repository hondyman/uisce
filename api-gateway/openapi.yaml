openapi: 3.0.0
info:
  title: SemLayer API Gateway
  version: 1.0.0
  description: API Gateway for SemLayer microservices

paths:
  /health:
    get:
      summary: Health check
      operationId: getHealth
      responses:
        '200':
          description: Service is healthy
