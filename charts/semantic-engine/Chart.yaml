apiVersion: v2
name: semantic-engine
description: Semantic Engine - Core semantic layer processing and query translation
type: application
version: 1.0.0
appVersion: "1.0.0"

maintainers:
- name: Semlayer Platform Team
  email: platform@semlayer.io

dependencies:
- name: common
  version: "1.x.x"
  repository: "https://charts.bitnami.com/bitnami"
  condition: common.enabled

annotations:
  category: analytics
  component: core
