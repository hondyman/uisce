# =============================================================================
# Shared environment for the split architecture
# =============================================================================
# Remote host (Tailscale IP of the server running infrastructure + Postgres)
REMOTE_HOST=100.84.126.19

# Postgres (runs on REMOTE host, outside compose)
POSTGRES_PASSWORD=postgres

# Auth
JWT_SECRET=dev-jwt-secret-key-change-in-production
HASURA_ADMIN_SECRET=myadminsecret
